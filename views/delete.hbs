<section id="create-section">
    <h1 class="item">Delete Accomodation</h1>
    <main class="item padded align-center">
        <form action="/room/{{room._id}}/delete" method="post" class="layout left large">
        {{#if error}}
        <div class="error-box">
            {{#each error}}
            <p>{{this}}</p>
            {{/each}}
            </div>
        {{/if}}
        {{#with room}}
            <div class="col aligned">
                <label><span>Name</span><input type="text" name="name" value="{{name}}" disabled></label>
                <label><span>City</span><input type="text" name="city" value="{{city}}" disabled></label>
                <label><span>Beds</span><input type="number" name="beds" value="{{beds}}" disabled></label>
                <label><span>Price per night</span><input type="number" name="price" value="{{price}}" disabled></label>
                <label><span>Image Url</span><input type="text" name="imageUrl" value="{{imageUrl}}" disabled></label>
            </div>
            <div class="content pad-med align-center vertical">
                <label>
                    <span>Description</span>
                    <textarea name="description" disabled>{{description}}</textarea>
                </label>
                <div class="align-center">
                    <input type="submit" class="action" value="Confirm Delete">
                </div>
            </div>
            {{/with}}
        </form>
    </main>
</section>